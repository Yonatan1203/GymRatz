source "https://rubygems.org"

# fastlane drives TestFlight upload + (later) tester management and App Store
# submission via the App Store Connect API key. Exact versions are pinned by the
# committed Gemfile.lock; CI runs `bundle install --frozen`.
gem "fastlane"
